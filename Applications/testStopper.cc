#include "ORLogger.hh"
//#include "ORProcessStopper.hh"

using namespace std;


int main()
{
  /*
  ORProcessStopper processStopper;
  processStopper.ExecuteStopperThread();

  int i = 0;
  while (1) {
    if (processStopper.Stop()) {
      if(processStopper.StopNow()) {
        cout << "stopping now." << endl;
        break;
      } else {
        cout << "quitting after run..." << endl;
        break;
      }
    }
    i++;
  }

  ORLog(kRoutine) << i << endl;

  processStopper.Join();
  */
  return 0;
}

